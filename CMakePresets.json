{
  "version": 6,
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Default (uses hog2 from ~/.local)",
      "binaryDir": "${sourceDir}/build"
    }
  ],
  "buildPresets": [
    {
      "name": "default",
      "configurePreset": "default"
    }
  ]
}
